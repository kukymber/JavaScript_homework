let a = null, b = 0;
alert(a==null); // true
alert(a==b);  // false
alert(b==0);  // true
// к переменой b присваевается значение 0, это число (number). 
// к переменой a присваевается ТИП ДАННЫХ null (umpty).
// нельзя сравнить значение и тип данных.
